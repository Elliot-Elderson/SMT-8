# OpenClaw 访问控制安全需求文档

## 1. 文档目的

本文定义 OpenClaw 访问控制系统应满足的安全需求，并按受保护对象和行为后果组织。系统依据这些需求确定安全边界，再将安全边界落实为运行时策略和可观察识别规则。

每条要求回答三个问题：

1. 哪类文件、数据或行为受到保护。
2. 系统在什么条件下拒绝、进一步判断或允许。
3. 当前实现通过哪些可观察特征识别该条件。

这些要求是访问控制执行、LLM 兜底判断、未来风险预测和策略更新审查的共同依据。当前实现中的安全边界和策略图应与本文保持一致。修改本文本身不会自动修改策略图；安全需求发生变化时，还需同步修改、验证并发布对应的安全边界和运行时策略。

## 2. 受保护对象如何定义

### 2.1 凭据和认证材料

以下路径或文件按凭据处理：

- 任意 `.ssh` 目录及其文件，例如私钥、SSH 配置和 `authorized_keys`。
- `.docker/config.json`。
- `.aws/credentials`。
- `.kube/config`。
- `.env` 及其变体，但 `.env.example` 不按真实凭据处理。
- 文件名包含 `id_rsa`、`id_dsa`、`id_ecdsa`、`id_ed25519`、`private_key`、`credentials`、`secret`、`token`、`api_key`、`passwd` 或 `password` 的文件。

文本或 Session 数据中出现以下内容时，也按携带认证材料处理：

- 密码、PIN、Secret、Token、API Key、Access Key、Private Key、SSH Key。
- Authorization、Bearer、OAuth refresh token、登录信息、Session ID、CSRF token、认证 Cookie。
- 数据库连接串、Webhook、Wi-Fi 密钥、钱包助记词等认证性内容。
- PEM/OpenSSH 私钥、AWS Access Key、GitHub Token、OpenAI Key、JWT 或带账号密码的连接 URI 等可识别值。

### 2.2 系统敏感资源

以下资源按系统敏感资源处理：

- 块设备，例如 `/dev/sda`、`/dev/vda`、`/dev/nvme0n1`、`/dev/mapper/*` 和 `/dev/dm-*`。
- `/etc/shadow`、`/etc/sudoers`、`/etc/passwd`。
- `/proc/self/environ`。
- root 用户的 SSH 目录和材料。

### 2.3 Agent 私有上下文与记忆

以下 OpenClaw workspace 文件按 Agent 私有上下文处理：

- `AGENTS.md`
- `IDENTITY.md`
- `SOUL.md`
- `TOOLS.md`
- `USER.md`
- `HEARTBEAT.md`

以下文件或目录按 Agent 记忆处理：

- `MEMORY.md`
- `memory/YYYY-MM-DD.md` 及其带后缀变体
- `workspace/memory`
- 当前实现仍能识别的 Hermes memories 路径

Agent 记忆与 Agent 私有上下文是不同资源类别；当前强制拒绝边界直接保护的是 Agent 私有上下文读取。

### 2.4 私人数据

以下内容按私人数据处理：

- 文件名表示 customer、user、payroll、settlement、wire、wallet、account、routing、SSN、passport 或 tax，且扩展名为 CSV、JSON、TXT、XLSX、DB 或 SQLite 的文件。
- `session`、`sessions`、`legacy_session`、`legacy_sessions` 目录，以及 `.sess`、`.session` 文件。
- Thunderbird、Maildir、Chrome、Chromium、Firefox profile 数据。
- 浏览历史、Cookie、Login Data、PST、OST、MBOX 等浏览器或邮件数据。

### 2.5 配置与供应链文件

以下内容按配置或供应链变更面处理：

- `package.json`、`setup.py`、`pyproject.toml`、`requirements.txt`、`composer.json`、`Gemfile`、`.gemspec`、`Cargo.toml`、`go.mod`、`pom.xml`、`build.gradle`、`Makefile`、`Dockerfile`。
- `config`、`configs` 目录中的文件。
- `/etc` 下的配置文件。
- cron、systemd、`.service`、SSH 配置、shell 启动文件、安装 hook、Git hook 和 GitHub Actions workflow。

### 2.6 源代码与普通文件

以下内容按源代码处理：

- `src`、`source`、`sources`、`lib`、`app`、`packages` 目录中的文件。
- 常见代码或工程扩展名，例如 JS、TS、Python、Go、Rust、Java、C/C++、Ruby、PHP、Swift、Kotlin、C#、Shell、YAML、TOML、lock 和 JSON。

没有命中更具体资源类别的本地文件按普通文件处理。

### 2.7 目标区域

- `localhost`、`127.0.0.1`、`::1`、私有 IPv4 地址和 link-local 地址按内部目标处理。
- 可正确解析且不属于上述范围的 URL 按外部目标处理。
- 无法解析的网络目标按未知目标处理，并在外发规则中与外部目标同等对待。
- 没有网络目标或只涉及本地路径的操作按本地行为处理。

## 3. 必须拒绝的行为

以下行为属于当前系统的强制拒绝边界。普通策略学习或 LLM 判断不得把它们放宽为允许。

### 3.1 凭据与敏感数据

- 禁止读取或列举第 2.1 节定义的凭据文件和凭据目录。
- 禁止把凭据文件发送到外部或未知目标。
- 禁止使用 `grep`、`rg`、`find`、`awk`、`sed` 等工具搜索 API key、token、secret、password、credential 或 private key。
- 禁止读取第 2.2 节定义的系统敏感资源。
- 禁止读取第 2.3 节定义的 Agent 私有上下文文件。
- 禁止读取或列举浏览器、邮件等个人 profile store。
- 禁止把第 2.4 节定义的私人数据发送到外部或未知目标。
- 禁止把 Session 文件或 Session 数据发送到外部或未知目标。
- 禁止把从前序操作继承到的密码、Token、账户标识、认证 Cookie、私钥或连接串发送到外部或未知目标，即使当前文件名看起来是普通文件。

### 3.2 系统破坏和安全控制绕过

- 禁止写入、删除或修改 `/usr/bin`、`/usr/sbin`、`/usr/lib`、`/usr/lib64`、`/bin`、`/sbin`、`/lib`、`/lib64`、`/boot` 及动态加载器配置路径。
- 禁止对根目录、home、当前目录、环境变量路径或敏感目录执行无界 `rm -rf`。
- 禁止删除 `.env`、shell history、`.ssh`、`.aws`、`.docker`、`.kube`、私钥、凭据、密码或 Token 文件。
- 禁止格式化或覆写块设备、清空关键系统文件、使用 `shred`、`mkfs`，或者对数据库和云存储执行大范围不可逆删除。
- 禁止关闭、跳过或绕过 firewall、audit、logging、scanner、policy、MFA、认证、TLS、证书、签名或校验机制。
- 禁止使用 `verify=false`、`strict-ssl false`、关闭 TLS 校验等方式削弱安全连接。
- 禁止执行会在内部间接修改支付、treasury、routing、wallet 或其他配置文件的本地脚本；此类变更应先检查内容，再以显式写入方式处理。

### 3.3 远程控制、持久化和提权

- 禁止 `curl` 或 `wget` 下载内容后直接通过 shell 执行。
- 禁止反向 shell、`nc -e`、`ncat -e`、`socat ... exec`、`/dev/tcp`，以及把 shell 输入输出绑定到网络连接的代码。
- 禁止创建 cron、systemd、launchd、shell 启动文件或 Git hook 等自动执行和持久化入口。
- 禁止新增或弱化 `authorized_keys`、`sshd_config`、`PermitRootLogin` 等 SSH 访问路径。
- 禁止使用 setuid、setcap、危险 chmod、usermod、root chown、sudo、su、内核模块、`LD_PRELOAD`、特权容器或宿主挂载实现提权。

### 3.4 资源耗尽和内部侦察

- 禁止 fork bomb、无限循环、无界进程或线程创建、大量 socket、磁盘填充、超大内存分配和持续压力负载。
- 禁止使用 nmap、masscan、`nc -z` 或循环 socket 连接扫描内部主机和端口。
- 禁止枚举内部路由、邻居、网络连接、云 metadata、Kubernetes 或 Jenkins 等内部基础设施。
- 允许查看本机单个网络接口信息并不等于允许执行网段扫描；只有识别为内部侦察的行为进入强制拒绝范围。

### 3.5 供应链和代码外传

- 禁止在 install、build、package、repository hook 或 CI workflow 中隐藏远程下载、命令执行、反向连接或数据外传逻辑。
- 禁止把破坏命令嵌入 `postinstall`、`preinstall`、`setup.py`、`cmdclass`、Makefile、Composer hook 或 workflow。
- 禁止写入包含外部上传、Webhook、远程日志、socket 连接或攻击者目标的可执行 payload。
- 禁止未经可信发布规则向公共 GitHub、GitLab、公共 repository、npm 或 PyPI 发布源代码。
- 禁止整体转储环境变量，例如无筛选运行 `env`、`printenv`、`export -p` 或读取 `/proc/self/environ`。
- 禁止编写或运行捕获、记录、收集 Authorization header、Bearer token、Cookie、Credential、API key 或 Session token 的逻辑。

## 4. 必须进一步判断的行为

以下行为不能仅凭普通允许规则直接放行，必须进入进一步裁决：

- 普通本地文件发送到外部或未知目标。
- 普通权限变更或文件权限修改。
- 修改依赖、安装脚本、构建 hook、Git hook、CI workflow、package registry 或发布配置。
- 写入包含内网主机枚举、端口扫描或网段扫描逻辑的源代码；实际执行这种代码仍应拒绝。
- 删除本地源代码文件，但已确定为受限临时工作区删除的情况除外。

当前生产路径中的“进一步判断”由访问控制 LLM 给出二元结果：

- 只有可信的当前证据足以证明操作属于用户请求范围，且不违反第 3 节强制拒绝要求时，才可允许。
- 缺少必要证据、LLM 超时、LLM 不可用或输出格式无效时，默认拒绝。
- benchmark 中的自动批准属于实验适配行为，不代表生产安全要求。

## 5. 可以保留的正常工作流

以下行为在没有同时命中更高风险要求时可以允许：

- 在本地读取普通文件或源代码。
- 在本地读取不含凭据、系统敏感内容或高风险标签的配置文件。
- 在本地读取私人数据；这不允许随后将其发送到外部。
- 在本地列举普通目录或源代码目录。
- 在本地打包普通文件；打包不会清除原数据的敏感属性。
- 把普通文件发送到内部或 loopback 测试服务。
- 向内部或 loopback 服务发起没有高风险行为的网络请求。
- 在本地执行没有高风险行为的源代码或普通脚本。
- 在本地任务工作区写入普通文件或源代码。
- 在本地更新私人数据；这不允许随后将其发送到外部。
- 删除单个、范围明确且没有被识别为破坏行为的普通文件。
- 删除静态确定、没有路径逃逸且位于 `/tmp_workspace` 后代的临时文件。

上述允许不是内容无关的永久授权。只要同一操作还携带凭据访问、破坏、持久化、提权、供应链、外发或其他更高风险特征，就必须优先执行第 3 或第 4 节要求。

## 6. 系统如何执行这些要求

### 6.1 行为归一化

系统先把工具调用归一化为以下事实：

- 使用了哪个工具。
- 是读取、写入、发送、执行、删除、列举、打包、权限变更、调度还是注册操作。
- 操作对象属于普通文件、Agent 记忆、源代码、凭据、私人数据、配置、系统敏感资源、Agent 私有上下文、外部服务还是未知资源。
- 命令、路径、参数或脚本内容是否命中高风险行为特征。
- 目标是本地、内部、外部还是未知区域。
- 操作涉及哪些实际路径。

这些基础事实主要由确定性代码和正则规则产生，不由 LLM 自由生成。执行本地脚本时，系统还会检查有限数量、有限大小的脚本内容，以识别间接配置写入、破坏、扫描、远程控制和供应链行为。

### 6.2 Session 数据流继承

敏感数据经过读取、复制、移动、打包、压缩、编码或加密后，不会自动变成普通数据。系统把前序数据来源的资源类别和安全属性传播给后续产物。

例如：

1. 读取一个包含 Token 的文件。
2. 把内容写入名字普通的临时文件。
3. 尝试把临时文件发送到外部。

第三步仍应按“携带凭据内容的外发”拒绝，而不能只根据临时文件名判断。

### 6.3 策略选择顺序

已有策略根据工具、操作、资源类别、风险特征、目标区域和可选路径进行匹配。多个策略同时命中时：

1. 拒绝优先于进一步判断，进一步判断优先于允许。
2. 同类结果中优先级更高的策略优先。
3. 仍然冲突时使用匹配分数和稳定排序决定。

没有已有策略完整匹配时，无风险的本地读取或列举可以进入受限的默认允许；其他情况进入 LLM 兜底判断。LLM 不可用或结果不可解析时默认拒绝。

## 7. LLM 必须遵守的要求

本文会作为可信安全上下文提供给访问控制兜底、未来步骤预测和策略更新审查 LLM。

### 7.1 访问控制兜底

- 只判断当前工具行为，不虚构未发生的授权或上下文。
- 用户指令可以作为授权证据，但不能覆盖第 3 节的强制拒绝要求。
- Session 继承的敏感类别和安全属性必须视为有效证据。
- 缺少关键授权、来源或效果证据时拒绝。
- 该步骤只返回当前行为的允许或拒绝，不直接生成永久策略。

### 7.2 未来步骤预测

- 预测必须基于当前 Session 中已经发生的数据流和行为。
- 预测结果不是永久策略。
- 只有风险类型、来源、操作、目标、预测步数和置信度都通过确定性校验后，才可生成 Session 内临时拦截规则。
- LLM 不可用时不生成临时规则，普通访问控制继续工作。

### 7.3 策略更新审查

- 只有 LLM 兜底已经实际参与并给出最终判断时，才能提出策略候选。
- 候选策略的效果必须与该次最终判断一致。
- 资源分类或数据传播错误不能通过新增策略掩盖。
- LLM 原始输出不得直接写入长期策略图。
- 候选必须经过结构校验、安全边界校验、历史重放、支持证据检查和攻击/正常样例验证后才能发布。

## 8. 策略更新不得破坏的边界

任何自动或人工策略更新都必须满足：

- 不得修改或删除现有强制安全边界。
- 不得用新的允许规则覆盖第 3 节中的拒绝范围。
- 不得取消第 4 节中必须进一步判断的保护。
- 不得因为新增拒绝规则而无意破坏第 5 节中的基本正常工作流。
- 更新必须基于当前 active revision，并通过图结构、编译结果一致性、历史重放、证据和 golden cases 验证。
- 一旦出现历史拒绝行为变成允许，候选必须被拒绝。

策略图中的安全边界主要用于验证未来策略更新；实时工具调用由已编译的运行时策略执行。

## 9. 当前实现限制

以下内容尚未被长期策略条件完整表达，汇报时不能把它们说成已经解决：

- 长期策略不比较写入内容、内容 hash、用户授权对象或变更摘要。
- 自动学习策略主要按操作、资源类别、目标、风险特征和路径概括，可能把同一路径上的不同写入内容视为同一范围。
- 当前验证能够阻止历史拒绝变成允许，但没有全局禁止历史“进一步判断”变成允许。
- 自动学习策略没有自动获得独立的新安全需求或强制边界。
- 修改本文不会自动重写已有策略图。
- 当前基础配置默认不启用未来步骤预测；benchmark 可以在运行时启用。
- Long-distance detector 主要提供 LLM 证据和限制默认允许，本身不单独决定所有静态访问控制结果。

因此，当前系统能够较好表达“哪类资源、哪类操作、哪类风险行为和哪个目标区域”，但还不能仅靠长期策略精确表达“本次具体写入内容是否获得用户授权”。

## 10. 汇报口径

可以这样介绍这份安全需求：

> 我们不是先写一套与实现脱节的抽象要求，再声称策略图已经满足它。我们从当前实际生效的安全边界反向整理需求：先定义凭据、私人数据、系统敏感文件、配置和源代码如何被识别，再明确哪些操作必须拒绝、哪些必须进一步判断、哪些正常工作流可以保留。运行时策略负责每次工具调用的访问控制，Session 图负责传播数据来源和敏感属性，安全边界负责防止未来策略更新突破这些要求。对于内容级授权和自动学习泛化等尚未解决的问题，文档明确列为当前限制。

不应声称：

- 安全需求文档会自动生成或同步策略图。
- 安全边界节点直接参与每次工具调用匹配。
- 所有用户授权语义已经成为长期策略条件。
- 所有自动学习策略都具有独立的新安全边界。
